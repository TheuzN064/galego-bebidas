# Upstash Redis (Vercel Marketplace integration)
# These are automatically injected by Vercel when you add the Upstash Redis integration
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Admin password (optional - will be set on first login if not provided)
ADMIN_PASSWORD=

# Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3000
